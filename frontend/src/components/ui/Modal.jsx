export default function Modal({ title, eyebrow, children, onClose, actions }) {
  return <div className="fixed inset-0 z-50 grid place-items-center bg-slate-900/35 p-4" role="presentation" onMouseDown={(event) => event.target === event.currentTarget && onClose()}><section className="w-full max-w-lg rounded-xl border border-slate-200 bg-white p-6 shadow-2xl" role="dialog" aria-modal="true" aria-labelledby="modal-title"><div className="flex items-start justify-between gap-4"><div><p className="mb-2 text-[10px] font-bold uppercase tracking-[0.16em] text-teal-700">{eyebrow}</p><h2 id="modal-title" className="font-display text-xl font-semibold text-slate-900">{title}</h2></div><button className="grid h-8 w-8 place-items-center rounded-full bg-slate-100 text-xl leading-none text-slate-500 transition hover:bg-slate-200" onClick={onClose} aria-label="Cerrar modal">x</button></div><div className="mt-5 text-sm leading-6 text-slate-600">{children}</div>{actions && <div className="mt-6 flex justify-end gap-2">{actions}</div>}</section></div>
}
