import { useMemo, useState } from 'react'
import Icon from '../../components/ui/Icon'
import { branches, inventoryProducts } from '../../data/dashboardData'

function StockProductList({ products, demandLaunched }) {
  return <section className="stock-product-list"><div className="stock-list-heading"><div><p className="panel-kicker">PRODUCTOS EN STOCK</p><h2>{demandLaunched ? 'Demanda proyectada a 30 días' : 'Inventario disponible'}</h2><p>La predicción usa las unidades vendidas en los últimos 30 días y la tendencia reciente.</p></div><span>{products.length} productos</span></div><div className="stock-list-table"><div className="stock-list-header"><span>PRODUCTO</span><span>STOCK ACTUAL</span><span>VENDIDOS 30 DÍAS</span><span>PREDICCIÓN 30 DÍAS</span><span>COBERTURA</span></div>{products.map((product) => <div className="stock-product-row" key={product.id}><span className="stock-product-name"><span className={`product-avatar ${product.tone}`}>{product.avatar}</span><span><strong>{product.name}</strong><small>{product.sku} · {product.category}</small></span></span><strong>{product.available} u.</strong><strong>{product.sold30} u.</strong><span><strong className="projected-demand">{Math.round(product.sold30 * (1 + Number.parseFloat(product.trend) / 100))} u.</strong><small>{product.trend} vs. periodo anterior</small></span><span><strong>{product.coverage} días</strong><small>{product.risk}</small></span></div>)}</div></section>
}

export default function PredictionsPage() {
  const [branchId, setBranchId] = useState(branches[0].id)
  const [view, setView] = useState('Resumen')
  const [demandLaunched, setDemandLaunched] = useState(false)
  const branch = branches.find((item) => item.id === branchId)
  const inStockProducts = useMemo(() => inventoryProducts.filter((product) => product.available > 0), [])
  const predictedDemand = (product) => Math.round(product.sold30 * (1 + Number.parseFloat(product.trend) / 100))
  const launchDemand = () => { setDemandLaunched(true); setView('Demanda') }

  return <div className="page-content branch-page"><section className="workspace-hero"><div><p className="eyebrow">MOTOR DE PREDICCION</p><h1>Selecciona una sucursal</h1><p className="subtitle">Consulta stock y demanda proyectada por identificador de sucursal.</p></div><label className="branch-id-field"><span>ID DE SUCURSAL</span><select value={branchId} onChange={(event) => setBranchId(event.target.value)}>{branches.map((item) => <option key={item.id} value={item.id}>{item.id}</option>)}</select></label></section><section className="branch-picker"><div className="branch-picker-heading"><div><p className="panel-kicker">SUCURSALES DISPONIBLES</p><h2>Elige el punto de venta que quieres analizar</h2></div><span className="branch-count">{branches.length} sucursales</span></div><div className="branch-list">{branches.map((item) => <button className={`branch-card ${item.id === branch.id ? 'selected' : ''}`} key={item.id} onClick={() => setBranchId(item.id)}><span className="branch-marker">{item.name.slice(0, 1)}</span><span className="branch-card-copy"><strong>{item.name}</strong><small>{item.city}</small><small className="branch-code">{item.id}</small></span><span className={`branch-status ${item.status === 'Revision' ? 'attention' : ''}`}><i />{item.status}</span><Icon name="arrow" /></button>)}</div></section><section className="branch-summary"><div className="branch-summary-header"><div><p className="panel-kicker">SUCURSAL ACTIVA · {branch.id}</p><h2>{branch.name} <span>{branch.city}</span></h2></div><div className="view-tabs" role="tablist">{['Resumen', 'Stock', 'Demanda'].map((item) => <button role="tab" aria-selected={view === item} className={view === item ? 'selected' : ''} key={item} onClick={() => { setView(item); if (item === 'Demanda') setDemandLaunched(true) }}>{item}</button>)}</div></div><div className="branch-kpis"><article><span>PRODUCTOS</span><strong>{branch.products}</strong><small>catalogados</small></article><article><span>VALOR EN STOCK</span><strong>{branch.stockValue}</strong><small>inventario actual</small></article><article><span>DEMANDA 30 DIAS</span><strong>{branch.demand}</strong><small>proyeccion estimada</small></article></div>{view === 'Resumen' && <div className="branch-insight"><span className="status-pulse" /><span>La sucursal tiene <strong>2 productos en riesgo</strong>. Revisa la cobertura antes de generar la proxima orden.</span><button onClick={launchDemand}>Lanzar demanda 30 días <Icon name="arrow" /></button></div>}{view !== 'Resumen' && <div className="branch-data-table"><div><span>PRODUCTO</span><span>{view === 'Stock' ? 'STOCK DISPONIBLE' : 'DEMANDA 30 DIAS'}</span><span>ESTADO</span></div>{inStockProducts.map((product) => <div key={product.id}><span><strong>{product.name}</strong><small>{product.sku}</small></span><strong>{view === 'Stock' ? `${product.available} u.` : `${predictedDemand(product)} u.`}</strong><span className={`risk-badge risk-${product.risk.toLowerCase()}`}>{product.risk}</span></div>)}</div>}</section><StockProductList products={inStockProducts} demandLaunched={demandLaunched} /></div>
}
